# Optional third-party deps for Forge brain verification (CI / local).
# The tools/verify suite and unittest targets are stdlib-only today — this file may stay empty.
# Install (when entries exist): pip install -r tools/verify/requirements-verify.txt
