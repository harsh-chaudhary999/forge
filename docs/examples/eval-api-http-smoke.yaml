# Example: minimal API eval (copy to brain/prds/<task-id>/eval/ and edit URL).
scenario: stack-smoke-api
description: One HTTP health check to prove eval wiring and stack-up.
preconditions: []
steps:
  - id: "step_1"
    driver: "api-http"
    action: "call"
    method: "GET"
    url: "http://localhost:3000/health"
    timeout_ms: 5000
    expected:
      status: 200
postconditions: []
metadata:
  tags: ["smoke", "example"]
